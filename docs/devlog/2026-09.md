# OrbitFlow Development Log — 2026-09

Historical implementation record for September 2026.

For the concise current source of truth, read `CURRENT_STATE.md`.

## 2026-09-21 — VLAN observation capability design baseline

**Completed**
- Added a dedicated `.agents/skills/vlan-observation/SKILL.md` for the next read-only multi-vendor VLAN capability.
- Locked the capability boundary to observed facts only: per-interface VLAN references plus VLAN database/equivalent service objects; consistency/policy analysis remains a separate future layer.
- Recorded approved configuration sources for Cisco IOS/IOS-XE, Huawei VRP, and Ubiquiti EdgeSwitch; IOS-XR command selection remains explicitly unapproved pending discussion.
- Added confirmed Cisco IOS/IOS-XE, Huawei NE05E, and EdgeSwitch VLAN parsing semantics to their vendor skills.
- Updated `AGENTS.md` routing and permanent observation-vs-policy rule.
- Updated `CURRENT_STATE.md` to record successful live interface validation across all five platforms and set VLAN observation as the next development focus.

**Validation**
- Documentation/source-of-truth change only; no runtime code or equipment configuration changed.

### 2026-09-20 — Huawei tunnel canonical-name matching

- Extended only Huawei's description-to-brief canonical interface-name join so
  description-side `Tun` and `Tunnel` names match brief-side `Tunnel` names.
- Preserved the existing Ethernet, GigabitEthernet, and LoopBack canonical
  mappings, case-insensitive as-is matching for other interface families,
  original description-side names, exact subinterface suffixes, status
  normalization, approved commands, and strict missing-interface failure.
- Added deterministic coverage for all confirmed live families and examples,
  including direct `NULL0` and `Vlanif` matches and both abbreviated and full
  tunnel names.

### 2026-09-20 — Huawei loopback canonical-name matching

- Extended only Huawei's description-to-brief canonical interface-name join so
  description-side `Loop1` matches brief-side `LoopBack1` while the resulting
  interface record retains `Loop1`.
- Preserved the existing `Eth`/`Ethernet` and `GE`/`GigabitEthernet` mappings,
  status normalization, approved commands, and clear missing-interface failure.
- Added deterministic regression coverage for all three canonical mappings and
  the unchanged unmatched-interface error path.

### 2026-09-20 — Huawei two-column interface-description support

- Extended only the Huawei VRP interface adapter to recognize both approved
  `display interface description` table shapes: `Interface / PHY / Protocol /
  Description` and `Interface / Description`.
- For the description-only shape, the adapter runs the already-approved
  `display interface brief` fallback and joins normalized PHY status to
  descriptions using Huawei-specific canonical names. Description abbreviations
  `Eth` and `GE` match brief names `Ethernet` and `GigabitEthernet`; the original
  description-side name and exact subinterface suffix remain in output.
- Accepted the confirmed fixed VRP brief legend block and protocol state suffixes
  such as `up(s)` without weakening strict rejection of unknown legends or rows.
  PHY remains authoritative for normalized status.
- Retained PHY normalization (`up` -> up/up, `down` -> up/down, `*down` ->
  down/down), explicit command-rejection errors, and strict failures for
  malformed rows or description interfaces missing from brief output.
- Added deterministic coverage using the exact live two-column sample, including
  `GE0/2/8.2434`, plus full-name canonical joins, `LoopBack1`/`NULL0` with
  `up(s)`, the full live legend block, all three PHY states, missing joins, and
  malformed brief output.
- No Cisco, EdgeSwitch, transport, authentication, inventory, reporting, or
  hostname-discovery behavior changed, and no Huawei commands were added beyond
  the existing approved brief fallback.

### 2026-09-20 — Single-device interface live-validation utility

- Added a deliberately small integration utility that accepts one device target,
  platform, existing `DeviceCredentials`, and existing `TransportConfig`.
- The utility reuses `connect_device`, caller-owned `DeviceSession` lifecycle, and
  `InterfaceService`, then prints a concise summary of normalized records.
- Added a deterministic test confirming delegation, output, cleanup, and that the
  device password is not printed.
- No transport, capability, vendor command, or parser behaviour changed. Inventory,
  Excel, concurrency, reporting, REST, and provisioning remain out of scope.

### 2026-09-20 — Interface status normalization corrections

- Corrected Huawei VRP normalization so PHY `down` remains administratively up
  and operationally down, while `*down` is both administratively and
  operationally down. The Protocol column no longer overrides physical state.
- Corrected Cisco IOS-XR administrative-down spelling normalization for
  `admin-down`, `admin down`, and `administratively down`.
- Added focused parser regressions; the full suite passes 54 tests.

### 2026-09-20 — Reusable multi-vendor interface capability

**Objective**
- Implement the first reusable observation capability for interface descriptions and status across all five supported platform identifiers.

**Relevant skills**
- `interface-collector`, `jumphost-connectivity`, `cisco-network-cli`, `huawei-network-cli`, and `ubiquiti-network-cli`.

**Implementation**
- Added a normalized immutable `InterfaceRecord` and transport-agnostic `InterfaceService` suitable for workflows and future API consumers.
- Added isolated Cisco IOS/IOS-XE, IOS-XR, Huawei VRP, and Ubiquiti EdgeSwitch command/parser adapters.
- Reused caller-owned `DeviceSession` objects and preserved the validated `CiscoIOSCLI` prompt/echo behavior.
- Added vendor paging setup and explicit rejection detection. Adapters execute only approved primary collection commands and do not guess alternatives after rejection.
- Kept unavailable EdgeSwitch administrative status empty and made empty output distinct from malformed non-empty output.
- Deliberately excluded inventory, Excel tracking, concurrency, REST, and provisioning.

**Validation / Tests**
- Added deterministic tests for each platform covering paging, approved commands, normal and empty output, descriptions containing spaces, status normalization, malformed output, rejected setup, and rejected collection commands.
- `PYTHONPATH=src pytest -q` passes 50 tests.
- `ruff check src tests`, `ruff format --check src tests`, `mypy src`, `python -m compileall -q src tests`, and `git diff --check` pass.

**Known issues / Limitations**
- New IOS-XR, Huawei, and EdgeSwitch adapters have deterministic mocked coverage but no live-device validation yet.
- Huawei brief-output enrichment and the supported EdgeSwitch description fallback are not invoked because primary output supplies the fields currently modeled; rejection never triggers an automatic alternate command.

**Next step**
- Add inventory-driven workflow orchestration and tracking/reporting as separate layers consuming this capability.

### 2026-09-20 — Reusable Cisco IOS/IOS-XE interactive CLI

**Objective**
- Provide prompt-aware, reusable command execution above the existing device transport.

**Prompt / Request**
- Detect IOS prompts dynamically, disable paging, clean command output, and enforce read timeouts.

**Relevant skills**
- `.agents/skills/cisco-network-cli/SKILL.md`

**Files changed**
- `src/orbitflow/vendors/cisco/ios.py`, `src/orbitflow/vendors/cisco/__init__.py`
- `tests/test_cisco_ios_cli.py`, `README.md`, `DEVLOG.md`

**Implementation**
- Added an IOS/IOS-XE-specific interactive CLI that consumes `DeviceSession` without changing transport.
- Prompt reads use channel timeouts and monotonic deadlines rather than fixed sleeps.
- Paging is disabled at initialization and command echo, ANSI control sequences, and the trailing prompt are removed from results.
- Live validation found that a stale prompt could complete a newly sent command before its response arrived; command reads now synchronize on the command echo before accepting the final prompt.

**Validation / Tests**
- Unit coverage includes `#` and `>` prompt detection, paging setup and command execution, timeout behavior, dynamic prompt changes, and output cleaning.
- Added regression coverage for a stale prompt arriving before the paging command echo.
- Windows live-device validation passed on Cisco ASR920 `NSW-STLEON-21CANB-BAS1` using IOS XE 17.06.07.
- The live test confirmed correct prompt detection, successful `terminal length 0`, complete `show version` output, removal of the command echo and trailing prompt, and clean session teardown.
- This live validation confirms the stale-prompt regression is fixed in the merged implementation.

**Known issues / Limitations**
- The implementation is intentionally limited to IOS/IOS-XE; IOS-XR requires a separate CLI implementation.

**Next step**
- Integrate the CLI with a separately scoped Cisco collection workflow.

### 2026-09-20 — Standardized permissive host-key defaults

**Objective**
- Standardize the validated Windows and Linux behavior while retaining configurable strict verification.

**Prompt / Request**
- Default bastion and target-device verification to permissive handling, remove Teleport-specific known-host loading, and do not implement custom Teleport CA verification.

**Relevant skills**
- `.agents/skills/jumphost-connectivity/SKILL.md`

**Files changed**
- `src/orbitflow/transport/models.py`, `src/orbitflow/transport/linux.py`
- `tests/test_transport.py`, `README.md`, `DEVLOG.md`

**Implementation**
- Both `verify_bastion_host_key` and `verify_device_host_key` now default to `False`, using Paramiko's permissive `AutoAddPolicy` without persisting learned keys.
- Removed the attempted `~/.tsh/known_hosts` loading; no custom Teleport CA verification is implemented.
- Configurable strict mode remains available and loads normal system host keys with `RejectPolicy`.
- The Windows local-forward architecture and Linux `tsh proxy ssh` plus `direct-tcpip` architecture are unchanged.

**Validation / Tests**
- Windows and Linux live validation succeeded using permissive host-key handling.
- Added mocked coverage for permissive defaults and the retained Linux strict bastion mode.

**Known issues / Limitations**
- Permissive verification does not protect against machine-in-the-middle attacks.

**Next step**
- Provision trusted system host keys and enable strict verification where operationally practical.

### 2026-09-20 — Configurable SSH host-key verification

**Objective**
- Allow bastion and target host-key verification to be configured independently.

**Prompt / Request**
- Preserve strict verification when enabled, while allowing unknown keys without a `known_hosts` prerequisite when disabled.

**Relevant skills**
- `.agents/skills/jumphost-connectivity/SKILL.md`

**Files changed**
- `src/orbitflow/transport/models.py`, `src/orbitflow/transport/linux.py`, `src/orbitflow/transport/windows.py`
- `tests/test_transport.py`, `README.md`, `DEVLOG.md`

**Implementation**
- Added independently configurable Linux bastion and target-device verification settings.
- Both Windows and Linux target connections honor the device setting without changing either validated Teleport path.
- Strict mode loads system host keys and rejects unknown keys; permissive mode accepts unknown keys without loading `known_hosts`. Both settings now default to permissive handling as recorded above.

**Validation / Tests**
- Added mocked coverage for enabled and disabled verification on the Linux bastion and on Windows/Linux target devices.

**Known issues / Limitations**
- Permissive verification does not protect against machine-in-the-middle attacks and should only be used when trusted host keys cannot be provisioned.

**Next step**
- Provision trusted target host keys and enable strict device verification where operationally practical.

### 2026-09-20 — Initial Python transport layer

**Objective**
- Provide one common device connection API over the validated Windows and Linux Teleport paths.

**Prompt / Request**
- Implement transport only, with mocked tests, safe cleanup, and no login/OTP automation.

**Relevant skills**
- `.agents/skills/jumphost-connectivity/SKILL.md`

**Files changed**
- `src/orbitflow/transport/*`, `src/orbitflow/__init__.py`, `tests/test_transport.py`
- `requirements.txt`, `README.md`, `.gitignore`, `DEVLOG.md`

**Implementation**
- Added `connect_device`, OS-specific Windows and Linux backends, typed settings, an owned session, and transport exceptions.
- Windows uses a temporary `tsh ssh -N -L` process; Linux uses `tsh proxy ssh`, Teleport key plus certificate, and `direct-tcpip`.
- Rejects unknown SSH host keys and closes resources on success and failure paths.
- Uses the target address, rather than the loopback forward address, for Windows SSH host-key lookup.
- Classifies Linux identity, proxy, and bastion failures as Teleport errors without exposing underlying authentication details.

**Validation / Tests**
- `PYTHONPATH=src pytest -q` passes 6 mock-only tests for both OS paths.
- `ruff check src tests`, `ruff format --check src tests`, `python -m compileall -q src`, and `git diff --check` pass.

**Known issues / Limitations**
- No live-device validation was performed at this initial implementation stage.
- Linux callers must discover and supply key/certificate paths from their active `tsh` profile.

**Next step**
- Integrate an approved credential provider and active-profile path discovery in a separately scoped task.


### 2026-09-20 — Device capability and future OSS architecture

**Objective**
- Define OrbitFlow's long-term architecture so reusable network-equipment functions can support both internal automation workflows and future external REST/OSS consumers.

**Prompt / Request**
- Separate reusable device operations from higher-level features, normalize device state into structured outputs, support deterministic analysis/remediation/troubleshooting workflows, and ensure the same capabilities can later be exposed through REST APIs as an OSS layer.

**Files changed**
- `AGENTS.md`
- `CURRENT_STATE.md`
- `docs/architecture/device-capability-oss-model.md`
- `docs/devlog/2026-09.md`

**Architecture / Decisions**
- OrbitFlow will evolve around reusable device capabilities rather than one-off workflow scripts.
- Vendor-specific commands and parsers remain isolated behind common capability-oriented interfaces.
- Higher-level features compose normalized device capabilities for audit, troubleshooting, provisioning, remediation, service assurance, and other operational tasks.
- Observation, analysis, planning, configuration application, verification, and recording remain separable.
- Future REST APIs, GUIs, schedulers, and OSS/BSS integrations must call the same internal application/capability interfaces instead of duplicating device logic.
- Normalized structured models should be reusable by workflows, reports, JSON/API responses, and future integrations.
- Raw vendor CLI should not become the primary external OSS abstraction.

**Validation / Tests**
- Documentation/architecture change only; no runtime behaviour changed.

**Known issues / Limitations**
- Exact Python interfaces, model classes, API framework, and endpoint schemas are intentionally deferred until capabilities are implemented incrementally.

**Next step**
- Use the interface-description collector as the first implementation that follows the reusable capability -> normalized model -> workflow/reporting pattern.

## 2026-09-20 — Dynamic interface device-name discovery

**Completed**
- Made `InterfaceService.collect` accept only device IP and platform as required
  identity inputs, retaining an optional explicit device-name override.
- Added vendor-specific hostname extraction from the prompts already detected
  for IOS/IOS-XE, IOS-XR, Huawei VRP, and EdgeSwitch without adding commands or
  changing transport behavior.
- Updated the single-device live-validation utility and deterministic tests for
  all supported prompt formats, including IOS-XR route-processor prompts.

**Validation**
- Full automated tests and repository quality checks passed.

## 2026-09-20 — Windows tunnel readiness and IOS-XR timestamp parsing

**Completed**
- Kept the validated Windows `tsh ssh -N -L` transport and replaced TCP-listener
  readiness with an SSH-banner probe over a disposable socket, so the separate
  socket later handed to Paramiko is not consumed or corrupted.
- Added deterministic coverage for delayed banners, non-SSH listeners, and
  accepted SSH 2.0 / 1.99 identification banners.
- Added an IOS-XR-specific parser entry point that ignores the known device
  timestamp before the interface table, including the live example
  `Sun Sep 20 20:15:28.579 AEST`.
- Preserved the strict shared IOS/IOS-XE parser and strict IOS-XR handling for
  unexpected non-empty lines after the table begins.
- Did not change commands, hostname discovery, inventory, reporting, REST,
  concurrency, or provisioning.

**Validation**
- `PYTHONPATH=src python -m pytest -q` passed 72 tests.
- Repository lint, formatting, compilation, typing, and whitespace checks
  passed.

**Known limitations**
- The fixes have deterministic regression coverage but were not revalidated
  against a live device during this change.

## 2026-09-20 — Windows SSH-banner probe follow-up

**Completed**
- Corrected the Windows readiness probe to retain a successfully connected
  disposable socket while waiting for the SSH identification instead of
  reconnecting after each short receive timeout.
- Accumulated fragmented data across reads and accepted legal pre-banner lines
  before an SSH 2.0 or 1.99 identification line.
- Limited reconnects to probe connection failures or EOF and preserved the
  separate socket subsequently opened for Paramiko.
- Preserved the existing Teleport transport architecture, IOS-XR parser, and
  device commands unchanged.

**Validation**
- `PYTHONPATH=src python -m pytest -q` passed 73 tests.
- Repository lint, formatting, compilation, typing, and whitespace checks
  passed.

**Known limitations**
- No live-device revalidation was performed during this follow-up.

## 2026-09-20 — Shared target keyboard-interactive authentication

**Completed**
- Added one shared target-device authentication helper used by both the
  validated Windows local-forward and Linux direct-tcpip transport paths.
- Preserved normal password/private-key authentication as the first attempt and
  added a keyboard-interactive retry with the existing device password after
  password authentication is rejected.
- Restricted interactive answers to prompts explicitly containing `password`;
  unrelated challenges fail authentication instead of receiving a secret.
- Kept `DeviceCredentials`, Teleport/bastion authentication, commands, parsers,
  hostname discovery, inventory, and reporting unchanged.
- Added deterministic coverage for password success, interactive success and
  failure, unrelated prompts, and Windows/Linux failure cleanup.

**Validation**
- `PYTHONPATH=src python -m pytest -q` passed 80 tests.
- Ruff, Black, compilation, and whitespace checks passed.
- Mypy remains limited by the environment's missing third-party Paramiko type
  stubs.

**Known limitations**
- The keyboard-interactive fallback has deterministic mocked coverage but was
  not validated against a live network device during this change.

## 2026-09-20 — Windows IOS-XR forwarded-socket handoff

**Completed**
- Preserved the Windows `tsh ssh -N -L` architecture while replacing the
  disposable SSH-banner readiness probe with a bounded local-forward connect
  retry.
- Retained the first successfully connected socket and passed that exact socket
  directly to the shared Paramiko target authentication helper without reading
  or consuming any SSH protocol data.
- Failed immediately when `tsh` exited and preserved cleanup of sockets, the
  Paramiko client, and the tunnel process on failure and normal session close.
- Kept the shared keyboard-interactive fallback and Linux transport unchanged.
- Added deterministic regressions for socket identity, absence of banner reads,
  startup retries and timeout bounds, `tsh` exit handling, and failure cleanup.

**Validation**
- `PYTHONPATH=src python -m pytest -q` passed 80 tests.
- Repository lint, formatting, compilation, typing, and whitespace checks
  passed.

**Known limitations**
- This change has deterministic mocked coverage but was not revalidated against
  a live Windows IOS-XR device in this environment.

## 2026-09-20 — EdgeSwitch live interface-status format

**Completed**
- Replaced the EdgeSwitch interface command with the confirmed
  `show interfaces status all` command after the existing
  `terminal length 0` setup, without adding fallback commands.
- Added deterministic fixed-column parsing for the confirmed multi-line table,
  including blank names, short `3/1  Down`-style rows, normalized link state,
  empty admin state, and the known `Flow Control:Disabled` footer.
- Added hostname extraction for the confirmed `(hostname) #` prompt while
  preserving simple `hostname#` and `hostname>` prompts.
- Kept Cisco, Huawei, transport, authentication, inventory, and reporting
  behavior unchanged.

**Validation**
- `PYTHONPATH=src python -m pytest -q` passed 89 tests using the exact captured
  live output and prompt.
- Ruff, Black, compilation, mypy, and whitespace checks passed.

**Known limitations**
- No network connection was made during this change; deterministic tests use
  the operator-confirmed captured live EdgeSwitch output.

### PR #17 follow-up — exact EdgeSwitch header order

- Corrected the deterministic fixture and strict parser header definition to
  match the confirmed capture exactly: the first line begins with the `Link`
  column and the second line begins with `Port` and `Name`.
- Kept the approved commands, fixed-column row parsing, prompt handling,
  normalization, footer handling, and strict failure behavior unchanged.
- `PYTHONPATH=src python -m pytest -q` passed 89 tests; repository quality
  checks passed.

## 2026-09-21 — Multi-vendor VLAN observation capability

**Completed**
- Added reusable read-only `VlanService`, normalized VLAN snapshots, interface
  observations, and distinct VLAN/service object identities.
- Added isolated running-configuration adapters for Cisco IOS, IOS-XE,
  IOS-XR, Huawei VRP, and Ubiquiti EdgeSwitch using only the explicitly
  approved command per platform and the existing prompt-aware transport path.
- Covered classic VLAN databases and switchports, IOS-XE EVC service instances,
  IOS-XR tagged/untagged routed and L2 subinterfaces plus L2VPN bindings,
  Huawei switched VLANs/Vlanif/dot1q termination/VSI bindings, and EdgeSwitch
  PVID/participation/tagging facts.
- Kept VLAN identity separate from bridge-domain and VSI/service identity and
  deliberately omitted consistency/compliance decisions.
- Added deterministic VLAN list/range, parser, normalization, and command
  selection tests for every supported platform.

**Validation**
- `PYTHONPATH=src python -m pytest -q` passed 109 tests.
- Ruff, Black, compilation, and whitespace checks passed.

**Known limitations**
- The new capability has deterministic mocked coverage but was not validated
  against live network devices during this change.

### PR #18 follow-up — service identity and hierarchical bindings

- Preserved IOS-XR L2VPN indentation and used it to scope bridge-group,
  bridge-domain, interface, and routed BVI bindings correctly.
- Retained routed BVI bindings as normalized interface observations even when
  the BVI has no VLAN encapsulation statement.
- Normalized IOS-XE and IOS-XR bridge domains and Huawei VSIs consistently as
  service objects whose identities remain separate from VLAN IDs.
- Retained Huawei control VID and dot1q termination VID as distinct facts when
  both are configured.
- Added deterministic regressions for nested IOS-XR bridge domains, explicit
  BVI membership, equivalent service objects, and distinct Huawei termination
  facts. The full suite passed 110 tests without adding device commands.
